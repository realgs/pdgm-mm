object Lista2 {
    def find[A](list:List[A], patterns :List[A]) : List[A] = {
        (list,patterns) match {
            case (head) =>
            case (head::tail,phead::ptail) => 

        }
    }
}