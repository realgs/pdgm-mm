object Lista2 {
    def find(list:List[String], patterns :List[String]) : List[String] = {
        (list,pattern) match {
            case head::tail => 

        }
        list.filter(list.head)
    }
}